Extend the existing Lady Health Worker (LHW) module by implementing a complete 
"Menstrual Hygiene Support & Awareness System" fully integrated with the 
Follow-Up Agent, Triage Agent, Knowledge Agent, and the rest of the multi-agent 
infrastructure from the project ZIP.

IMPORTANT:
Nothing in this module may be hardcoded. All dynamic values, such as supply counts, 
donation accounts, seminar materials, regions, health categories, users, and 
instructional content must be fetched from the database.

===============================================================
MENSTRUAL HYGIENE SUPPORT MODULE (PRIMARY NEW FEATURE)
===============================================================

Create a dedicated system inside the LHW module to support:
- Menstrual hygiene awareness
- Sanitary pads distribution tracking
- Identification of families using unsafe menstrual materials
- Education sessions for menstrual health
- Donation-driven support for menstrual supplies

-----------------------------------------
1. DATABASE STRUCTURE (STRICT REQUIREMENTS)
-----------------------------------------

Add the following tables:

1. menstrual_hygiene_status
   - id
   - household_id (FK)
   - last_cycle_date (nullable)
   - uses_safe_products (boolean)
   - notes
   - lhw_id
   - created_at
   - updated_at

2. menstrual_pad_requests
   - id
   - household_id
   - lhw_id
   - quantity_requested
   - urgency_level (low, medium, high)
   - status (pending, approved, delivered)
   - created_at
   - updated_at

3. menstrual_education_sessions
   - id
   - household_id
   - lhw_id
   - materials_provided (linked to DB)
   - topics_covered (e.g., infections, cycle tracking, hygiene)
   - feedback_form (JSON)
   - created_at
   - updated_at

These tables MUST integrate with existing schema:
- households
- frontliners (LHW subtype)
- follow_up_schedules
- regions
- knowledge_agent_logs

-----------------------------------------
2. API ENDPOINTS (BACKEND)
-----------------------------------------

Create the following endpoints:

- GET /api/lhw/menstrual/household-status
- POST /api/lhw/menstrual/update-status
- POST /api/lhw/menstrual/request-pads
- GET /api/lhw/menstrual/requests
- POST /api/lhw/menstrual/create-education-session
- GET /api/lhw/menstrual/sessions

All endpoints must:
- Use authentication middleware already present in the project ZIP.
- Follow the existing controller/service/repository pattern.
- Respect the response format used elsewhere in the app.

-----------------------------------------
3. FRONTEND (LHW INTERFACE)
-----------------------------------------

Add UI pages aligned with app’s current UI architecture:

A. Menstrual Hygiene Dashboard
   - Households flagged as "unsafe menstrual materials"
   - Pending pad requests
   - Upcoming awareness sessions
   - Progress stats (fetched from DB)

B. Household Menstrual Profile Page
   - Cycle info
   - Hygiene status
   - Education provided
   - Button to request pads
   - Auto-suggestions generated by Knowledge Agent

C. Education Session Interface
   - Topics (fetched from DB)
   - Material selection (DB-driven)
   - Upload voice notes (optional)
   - Mark session as complete
   - Feedback form

D. Pad Distribution & Stock View
   - LHW sees allocated pads
   - Tracks deliveries
   - Requests more supplies dynamically (from donation-supported stock)

Must fully support offline-first logic:
- Cache household menstrual data
- Queue updates until connection is restored

===============================================================
INTEGRATION WITH DONATION PLATFORM (NEW REQUIREMENT)
===============================================================

You must integrate this menstrual module with the donation platform system 
(previously created), but now extend it with stricter *database-only* dynamic logic.

-----------------------------------------
1. Donation Accounts (Database Only)
-----------------------------------------

Use ONLY values stored in the "donation_accounts" table:

- No account number, IBAN, wallet number, bank name, QR, or text may be hardcoded.
- Donation purposes must be dynamic, stored in DB.

Add to donation_purposes table:
- menstrual_pads
- menstrual_hygiene_kits
- menstrual_awareness_sessions

Frontend must:
- Fetch donation purposes dynamically
- Fetch bank/wallet accounts from database dynamically
- Display donation options based SOLELY on DB data

-----------------------------------------
2. Pad Supply Allocation Flow
-----------------------------------------

A donation → creates allocation → becomes stock → LHW requests → LHW distributes

Create tables:

1. pad_supply_inventory
   - id
   - quantity_available
   - region_id
   - updated_at

2. pad_supply_allocations
   - id
   - request_id
   - quantity_allocated
   - allocation_status
   - timestamp

All allocation logic must follow the same structure used in vaccination supply flows 
in the existing project.

===============================================================
SEMINAR SCHEDULING INTEGRATION
===============================================================

Expand seminar system to include menstrual awareness workshops.

Add fields to seminars table:
- seminar_type (menstrual_hygiene, vaccination, maternal_care, general_health)
- required_materials (DB-driven)
- expected_attendance
- sponsoring_donation_id (nullable)

Add pages:
- Seminar Planner (LHW)
- Seminar Calendar
- Seminar Material Request
- Donor-Sponsored Seminars View

All seminar details must integrate with donation allocations at DB level.

===============================================================
AI AGENT INTEGRATION
===============================================================

 MUST integrate into the multi-agent architecture:

1. Knowledge Agent
   - Extract hygiene trend insights
   - Track infection risk in communities
   - Recommend high-need areas for pad donations & seminars

2. Follow-Up Agent
   - Remind LHW about:
     - households using unsafe materials
     - pending pad distribution
     - upcoming menstrual education sessions

3. Triage Agent
   - When LHW marks a case as "urgent menstrual health issue"
     → trigger triage workflow with medical advice and referral

===============================================================
SYNC WITH ENTIRE PROJECT ZIP STRUCTURE
===============================================================

All code must follow:
- Same backend folder structure
- Same frontend architecture
- Same naming conventions
- Same DB ORM models style
- Same middleware and auth flows
- Same mobile-first + offline-first design

ABSOLUTELY NOTHING can break the existing architecture.

===============================================================
OUTPUT REQUIREMENT
===============================================================

Generate:
- Full backend code
- Full frontend code
- Database migrations
- API controllers + services
- Models
- React/Flutter screens
- Admin panel pages
- All integration logic with agents

===============================================================
END OF PROMPT
===============================================================
